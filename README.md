# NEWSROOM

A pipeline-based AI newsroom project.

## Structure

- `backend/` — Python backend and article pipeline
- `frontend/` — HTML/CSS/JS user interface
- `.env` — environment variables for API keys
- `.gitignore` — ignored files
